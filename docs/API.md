# API

OpenAPI documentation is available at `/api/schema/`, `/api/docs/` and `/api/redoc/`.

The API currently includes read-only public catalog endpoints and authenticated customer endpoints for addresses, cart, orders and support. Future API work should keep all calculations in services under `apps/*/services.py`.
