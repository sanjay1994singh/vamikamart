# Checkout

`CheckoutService` calculates authoritative cart totals using Decimal: subtotal, coupon discount, GST-style tax, shipping and final total. Frontends should display these values, not recalculate them.
