# Payments

Payment models support COD and Razorpay-ready fields. Webhook events are logged with provider event uniqueness for idempotent processing. Signature verification and live provider calls still require credentials.
